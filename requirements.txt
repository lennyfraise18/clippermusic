# --- Interface ---
gradio>=5.9,<6

# --- Transcription alignée mot par mot ---
# faster-whisper plutôt que whisper-timestamped : même qualité, timestamps par
# mot inclus, mais sans PyTorch. Mesuré sur 154 s d'audio :
#     whisper-timestamped (PyTorch) : ~1900 Mo de RAM, ~70 s
#     faster-whisper (CTranslate2)  :  ~390 Mo de RAM,  ~9 s
# PyTorch réservait à lui seul plus d'un gigaoctet, ce qui faisait tuer le
# conteneur sur un hébergement limité en mémoire.
faster-whisper>=1.1

# --- Extraction de mots-clés ---
spacy>=3.8,<3.9

# --- Appels API (Pexels, Pixabay, Jamendo) ---
requests>=2.32
python-dotenv>=1.0
